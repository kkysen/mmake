export interface Tools {
    readonly valgrind: string;
    readonly callgrind: string;
    readonly massif: string;
    
    readonly mkdir: string;
    readonly rm: string;
    readonly find: string;
}